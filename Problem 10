#include <iostream>
#include <fstream>
using namespace std;

int main() {
    string filename;
    cin >> filename;

    ifstream fin(filename);

    char c;
    int cnt = 0;
    while (fin.get(c))
        cnt++;

    cout << cnt;
    return 0;
}
